<UserControl x:Class="Task_Logger_Pro.Pages.Views.ChartViews.DailyAppsUsageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Task_Logger_Pro"
             xmlns:controls="clr-namespace:Task_Logger_Pro.Controls"
             xmlns:chart="clr-namespace:De.TorstenMandelkow.MetroChart;assembly=De.TorstenMandelkow.MetroChart"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300">
    <UserControl.Resources>
        <local:ConvertMainWndWidthToHalf x:Key="convertWidth" />
        <local:ConvertBoolToVisibility x:Key="convertVisibility" />
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">
        <Grid>
            <chart:StackedColumnChart ChartTitle="Daily app usage"
                                      ChartSubTitle="Hours per day"
                                      Style="{StaticResource MinimalChartStyle}"
                                      SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                                      MinHeight="{Binding UserSettings.MainWindowHeight, Converter={StaticResource convertWidth}}"
                                      BorderBrush="{DynamicResource WindowBorderBrush}"
                                      BorderThickness="1"
                                      SeriesSource="{Binding WeakCollection.Target}"
                                      Margin="10,10,10,20"
                                      Width="Auto"
                                      Height="Auto">
                <chart:StackedColumnChart.SeriesTemplate>
                    <DataTemplate>
                        <chart:ChartSeries SeriesTitle="{Binding Date}"
                                           DisplayMember="AppName"
                                           ValueMember="Duration"
                                           ItemsSource="{Binding DailyUsedAppsCollection}" />
                    </DataTemplate>
                </chart:StackedColumnChart.SeriesTemplate>
            </chart:StackedColumnChart>
            <controls:progressBar Visibility="{Binding Working, Converter={StaticResource convertVisibility}}" />
        </Grid>
    </ScrollViewer>
</UserControl>
